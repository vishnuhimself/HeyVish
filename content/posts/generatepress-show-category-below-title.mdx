---
title: "GeneratePress: Show Category Below Title"
date: "2024-01-01T12:30:00Z"
description: "Learn how to move post categories below the title in GeneratePress theme. A simple code solution to improve your blog's content hierarchy."
category: ["WordPress", "GeneratePress"]
---

GeneratePress, by default, shows the Category below the post content. This is not ideal for most blogs. But there is a way to show the Category below the Title, like this:

<img 
  src="/postimages/GeneratePress-Category-Below-Title.png" 
  alt="GeneratePress Category Below Title Example" 
  className="rounded-sm border shadow-sm"
/>

## Code to Show Category Below Title in GeneratePress

```php
add_action( 'generate_after_entry_title', 'lh_add_cat_below_title' );
function lh_add_cat_below_title() {
    if (is_single() ) {
        $categories_list = get_the_category_list( _x( ', ', 'Used between list items, there is a space after the comma.', 'generatepress' ) );
        $categories_list = sprintf( '<span class="cat-links-below-title"><span class="screen-reader-text">%1$s </span>%2$s</span> | ',
            _x( 'Categories', 'Used before category names.', 'generatepress' ),
            $categories_list
        );
    }

    ?>
    <div class="entry-meta-cat">
    <?php echo $categories_list; ?>
    </div>
    <?php
}
```

## How to add this code to your WordPress site

<LinkCard 
  href="/blog/add-php-wordpress/"
  title="How to Add PHP Code to WordPress"
  isExternal={false}
/> 